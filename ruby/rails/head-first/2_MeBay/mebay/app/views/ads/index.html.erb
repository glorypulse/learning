<h1>All Ads.</h1>
  <ul>
    <% @ads.each do |ad| %>
    <li><a href="<%=ad_path(id: ad.id)%>"><%= ad.name %></a>
      [ <%= link_to 'Delete', ad, method: :delete, data: { confirm: 'Are you sure?' } %> ]
    </li>
    <% end %>
  </ul>
<p><a href="<%= new_ad_path %>">Add New Ad</a></a>
